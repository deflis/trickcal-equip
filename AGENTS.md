# AGENTS.md

- pnpmのプロジェクト。
- 作業が終わった場合、必ずLintとビルドの確認をすること。
- テストは同じディレクトリに`*.test.ts`という形式で同じファイル名で作成すること。
- 可能な限りTDDで進めること。
- React Compilerを使用しているため、メモ化は不要。
- [デザインガイド](DESIGN.md)

## [トリッカル・もちもちほっペ大作戦（グローバル版）](https://trickcal.biligames.com/jp/)

足りない情報はだいたい [トリッカル攻略 Wiki](https://wikiwiki.jp/thetrickal/) で入手できる。

### 進軍・微辛

- 各ステージにはドロップアイテムがあり、確率でゴールド、モカロン、装備の設計図が入手できる。
- キャンディ（スタミナ）の消費は、どのステージでも固定で10。
- 高いレベルのステージほど、より多くのゴールドやモカロンが入手できる。
- ランクの高い装備ほどドロップ率が低いらしい。

## アプリケーション仕様

### 装備システムの仕様 (`src/blueprints.ts`, `src/types.ts`, `src/stages.ts`)
このプロジェクトは、ゲーム内の装備ランクとそれに基づいた素材必要数を定義する。
- 各キャラクターには物理/魔法の二種類の装備がある。違いは武器の種類だけ。
- 装備の製作に必要な素材数は、当該ランクと一つ下のランクでそれぞれ定義されている。
- ステージごとにドロップする素材の種類も定義されている。
- 基本的には上位の素材の方がドロップしずらい。ステージの難易度が上がると、素材のランクも上がるので、上位の素材を副産物として集める方が効率が良くなるケースが多いので、それを推奨する。
- 素材ではない装備をそのまま入手できることもあるので、装備は個別に選べるようにする。
- 上位のステージで、不足素材数の少ないモノから集めることによって、集めすぎて効率が悪くなることを防ぐ。

### 最適化アルゴリズム (`src/logic/stageRecommendation.ts`)
- 不足している全素材を「最小のスタミナ（＝最小のステージ数）」で効率よく集めるためのルート計算ロジック。
- ワールドが進むほど、コインなどのこのリストでは管理していない副産物が増えたりするので、上位ステージで下位の素材を集めるのが効率的になるため、ワールドレベルが高い方が有利になるようなルート計算ロジックが必要。

### ステート・パイプライン (`src/store.ts`, `src/selectors.ts`)
- 値の保持と、selectorを通じた派生データの計算を行う。
- 複雑な値の変換がある場合はselectorsを活用し、テストを行う。

### 必要数について

- 最適ルートを計算した際は、最も少ない数のアイテムから集めた方が効率が良いので、最小のアイテムから表示する。
- 同じステージでの優先度は、低いランクの個数の少ないものから集めた方が効率が良いので、そちらに設定する。同じ時は優先を付けない。
